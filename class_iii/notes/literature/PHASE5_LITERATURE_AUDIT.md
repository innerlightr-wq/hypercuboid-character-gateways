# Class III Phase 5 — Literature Audit

Targeted re-search (web search only), per Phase 5 Part XVII. Builds on,
does not replace, the Round 2/3/4 audits (`ROUND2_LITERATURE_AUDIT.md`,
`ROUND4_EXTREMAL_K3_AUDIT.md`).

| Target | Result | Classification |
|---|---|---|
| Exact `Σ_III(p)` | Not found in any search across all rounds | **NOT FOUND** |
| Exact `T(p)` | Not found | **NOT FOUND** |
| Equivalent finite-field hypergeometric sums | Greene's hypergeometric framework covers structurally similar (not identical) sums; no exact match | **RELATED** |
| Exact `X_III` K3 model (this specific Weierstrass equation / 3×`I2*` fiber locations) | Not found | **NOT FOUND** |
| Three-`I2*` extremal K3 surfaces (general) | Miranda–Persson's 112-configuration classification and Shioda's 325-surface list almost certainly contain this configuration; specific table entry not accessed | **SAME FIBER CONFIGURATION** (indicated, unconfirmed) |
| `T=diag(2,2)` singular K3 (general) | Takatsu (arXiv:1903.03054) constructs a discriminant-4 singular K3 via a *different* (double-plane) model; Schütt's discriminant-4 framework exists generally | **SAME TRANSCENDENTAL LATTICE** (different construction) |
| `η^6(4z)` | Confirmed independently by Huber–Liu–McLaughlin–Ye–Yuan–Zhang as a CM-by-`Q(i)` weight-3 level-16 newform, associated (by them) with the quartic Fermat K3 surface — a *third* variety beyond AOP's `X_8` and this project's `X_III` | **SAME MODULAR FORM ONLY** |
| `16.3.c.a` | Same as above; also independently reconfirmed this phase that Livné's theorem statement (searched fresh) matches exactly the form used throughout this project ("if `X` is a singular K3 over `Q` with discriminant `d`, there exists a weight-3 newform with CM by `Q(√d)`...") | **PROVED — LITERATURE** (theorem statement itself, re-confirmed) |
| AOP `λ=8` surface | Different Weierstrass model/fiber arrangement from `X_III`; `T(p)≠A(8,p)` (killed, Round 1); shares the *same untwisted* `16.3.c.a` trace formula (Round 2 Part X) | **SAME MODULAR FORM ONLY**, geometrically **RELATED ONLY** |
| Schütt, "Arithmetic of a Singular K3 Surface" (Michigan Math. J. 56, 2008) | Found this phase; treats a specific named singular K3 surface (not confirmed to be discriminant 4 or `X_III`) — flagged for potential future reading, not chased further this phase (time-bounded search) | **RELATED** (unconfirmed relevance) |

**No novelty claim is made from any NOT FOUND result.** The exact sum,
the exact Weierstrass model, and the exact identification of `X_III` as a
third discriminant-4 singular K3 (alongside AOP's `X_8` and the quartic
Fermat K3) were not located in the literature searched, but this reflects
the limits of web search without institutional database/table access
(Schütt's complete discriminant tables, Shioda's full 325-surface list),
not a positive claim that no such reference exists.
